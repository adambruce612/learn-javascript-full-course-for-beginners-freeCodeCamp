// All comparison operators return a true or false value
function isLess(a, b) {
    //Fix this code - there is no need to use an if-else statement here as the comparison operator returns a boolean
    // if (a < b) {
    //     return true;
    // } else {
    //     return false;
    // }
    return a < b;
}

// Change these values to test
console.log(isLess(10, 15));