// Loops allow you to run the same code multiple times
// A while loop runs until a specified condition is true and stops as soon as that condition is no longer true
var myArray = [];

var i = 0;
while (i < 5) {
    myArray.push(i);
    i++;
}

console.log(myArray);