// For loops are the most common type of loop in JavaScript
// Example
var ourArray = [];

// Initialization, the condition, the final expression. The intialization happens before any of the code inside the loop runs.
for (var i = 0; i < 5; i++) {
    ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line. 
// Use a for loop to create an array with the values 1-5
for (var i = 1; i < 6; i++) {
    myArray.push(i);
}

console.log(myArray);